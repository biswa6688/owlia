export function Download() {
  return (
    <div className="min-h-screen bg-[var(--bg)] p-8 text-[var(--text)]">
      <h1 className="text-2xl font-semibold">Download</h1>
      <p className="mt-2 text-[var(--text-muted)]">ONNX model status + CLI management — pending (Epic 2, Epic 13)</p>
    </div>
  )
}
